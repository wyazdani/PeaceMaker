import React from 'react'
import Cv from '../../Images/csv.png'
import {Button} from "react-bootstrap";

const CSV = (props) => {
  return (
      <Button variant='csv'>
          All Reports
      </Button>
  )
}

export default CSV